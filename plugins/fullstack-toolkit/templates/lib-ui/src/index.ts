/**
 * @scope/ui - Shared UI library
 *
 * Usage:
 *   import { cn, colors } from '@scope/ui';           // Common utilities
 *   import { Button } from '@scope/ui/web';           // Web components
 *   import { Button } from '@scope/ui/native';        // Native components
 */

export * from './tokens';
export * from './utils';
