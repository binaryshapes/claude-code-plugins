$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'application'
language: 'typescript'
stack: 'backend'
tags: ['backend', 'api', 'orpc']

# Tasks (dev, build, start, test, lint, typecheck, clean) are automatically
# inherited from .moon/tasks/typescript-app.yml and .moon/tasks/tag-orpc.yml
