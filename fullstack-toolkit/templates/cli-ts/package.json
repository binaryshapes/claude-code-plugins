{
  "name": "@{{scope}}/{{name}}",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "{{bin}}": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "chalk": "^5.4.0"
  }
}
