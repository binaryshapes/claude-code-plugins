$schema: 'https://moonrepo.dev/schemas/project.json'

type: 'tool'
language: 'typescript'
tags: ['cli']

# Tasks (dev, build, test, lint, typecheck, clean) are automatically
# inherited from .moon/tasks/typescript-cli.yml
