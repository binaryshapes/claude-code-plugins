/**
 * Native components (React Native + NativeWind)
 *
 * Usage:
 *   import { Button } from '@scope/ui/native';
 *
 * Note: This subpath is not bundled - it imports directly from src
 * to support Metro bundler and NativeWind's className transform.
 */

export * from './components/button';
